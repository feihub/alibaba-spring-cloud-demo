单点登录//单一用户登录
微服务认证与鉴权
oauth2 认证服务器/资源服务器/认证客户端

API网关， 在API网关中认证鉴权，其它内部微服务不再认证鉴权， 这样认证鉴权只有一个点，业务解耦，微服务业务也功能单一，只处理自己业务，不管认证鉴权，认证鉴权的升级维护也只有这一个点。

认证鉴权也是一种服务，微服务核心点，服务要小，功能要单一。如果多个微服务各自都要包含认证鉴权，那么微服务业务会耦合，功能会复杂。

----->微服务，功能单一，且该功能只在一个微服务内做，其他微服务内不再做相关功能。

业务功能的统一和分散，单一还是多次调用功能
