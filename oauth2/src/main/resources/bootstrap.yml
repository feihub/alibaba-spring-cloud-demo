spring:
  application:
    name: oauth2

  cloud:
    config:
      discovery: 
        enabled: true
        serviceId: config
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8084/eureka/

  session:
    store-type: redis