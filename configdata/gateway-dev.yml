server:
  port: 8086

spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8082
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8084/eureka/

logging:
  level:
    org.springframework.cloud: 'DEBUG'
