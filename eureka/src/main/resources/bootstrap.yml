server:
  port: 8084

spring:
  application:
    name: eureka

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream

eureka:
      client:
        registerWithEureka: false
        fetchRegistry: false

logging:
  level:
    org.springframework.cloud: DEBUG
