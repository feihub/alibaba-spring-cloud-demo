server:
  port: 8084

spring:
  application:
    name: eureka

  zipkin:
    baseUrl: http://zipkin/
  sleuth:
    sampler:
      probability: 1.0

eureka:
      client:
        registerWithEureka: false
        fetchRegistry: false

logging:
  level:
    org.springframework.cloud: DEBUG
