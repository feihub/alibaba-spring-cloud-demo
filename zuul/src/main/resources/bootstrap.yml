spring:
  application:
    name: gateway
  cloud:
    config:
      discovery: 
        enabled: true
        serviceId: config
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8084/eureka/